import { PlaceholderScreen } from "../../shared/ui/PlaceholderScreen";

export default function ImagesScreen() {
  return (
    <PlaceholderScreen
      description="Generate images through OpenRouter. Jobs will appear here once the generation feature is wired up."
      title="Images"
    />
  );
}
